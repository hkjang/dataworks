# Data Works runtime compose file.
# Load dataworks-v0.9.53.tar.gz, set the four required variables in .env, then run docker compose up -d.
services:
  gateway:
    image: dataworks:v0.9.53
    container_name: dataworks
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      POSTGRES_DSN: ${POSTGRES_DSN:?POSTGRES_DSN is required}
      BOOTSTRAP_ADMIN: ${BOOTSTRAP_ADMIN:?BOOTSTRAP_ADMIN is required}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:?BOOTSTRAP_ADMIN_PASSWORD is required}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required}
